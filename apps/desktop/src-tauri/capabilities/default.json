{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window of Grok GUI",
  "windows": ["main", "session-*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "updater:default"
  ]
}
